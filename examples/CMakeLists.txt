# Executable study material lives here, outside core.
#
# core/ is the reusable engine library layer. examples/ is allowed to compose
# modules, benchmark them, and print study output.

add_subdirectory(core)
